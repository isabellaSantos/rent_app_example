<h2><%= t(:new_resource, resource: Order.model_name.human) %></h2>

<div class="row">
  <div class="col s4">
    <div class="card-panel center-align <%= @order.cart? ? 'teal white-text' : '' %>">
      <h6>Itens</h6>
    </div>
  </div>
  <div class="col s4">
    <div class="card-panel center-align <%= @order.delivery? ? 'teal white-text' : '' %>">
      <h6>Entrega</h6>
    </div>
  </div>
  <div class="col s4">
    <div class="card-panel center-align <%= @order.payment? ? 'teal white-text' : '' %>">
      <h6>Pagamento</h6>
    </div>
  </div>
</div>

<div class="card-panel">
  <%= render partial: @order.state %>
</div>
